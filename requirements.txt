# Compatible with Python 3.10+ (tested on Python 3.11 and 3.13)
# Headless build is required for Streamlit Cloud (avoids libGL dependency)
opencv-python-headless>=4.8.0
numpy>=1.26.0
Pillow>=10.0.0
streamlit>=1.30.0
